"""from sqlalchemy.orm import Session

from src.core.models.Copy import Copy

# on retourne toutes les copies de la base de données
def get_all_copies(db : Session):
    return db.query(Copy).all()"""
    